const config = {
  // DataBase config
  db : {
      host: "mongodb.host.com",
      port: "2717",
      name: "dbname",
      user: "username",
      pass: "password"
  },
  // Provider config
  provider : {
      port: "5000"
  }
}

module.exports = config;